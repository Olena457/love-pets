export const selectProfile = state => state.profile.profile;
// export const selectProfileFavorites = state =>
//   Array.isArray(state.profile.favorites)
//     ? state.profile.favorites
//     : Object.values(state.profile.favorites || {});
